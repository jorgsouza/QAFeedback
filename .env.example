# Copiar para `.env` na raiz (o `.env` está no .gitignore).
# Token: Figma → Settings → Security → Personal access tokens
# Permissões usuais: File content (Read), Dev resources (Read).
FIGMA_API_KEY=
